import { StackContext } from "@serverless-stack/resources";

/**
 * We use Cognito for simplicity, but you can use any auth provider like Auth0
 * @param param0 
 */
export function Auth({}: StackContext) {

}